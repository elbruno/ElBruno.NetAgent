<Window x:Class="ElBruno.NetAgent.UI.Views.AuditLogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:ElBruno.NetAgent.UI.Converters"
        mc:Ignorable="d"
        Title="Audit Log Viewer - DRY-RUN MODE"
        Width="900"
        Height="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <converters:DryRunColorConverter x:Key="DryRunColorConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="ElBruno.NetAgent — Audit Log"
                       FontSize="18" FontWeight="Bold" Foreground="#1a1a2e"/>
            <TextBlock Text="DRY-RUN MODE: No real network changes are being made"
                       FontSize="13" Foreground="#d32f2f" FontWeight="SemiBold" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Audit Log DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding LatestEntries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#cccccc"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="180"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="180"/>
                <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="140"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Is Dry Run" Binding="{Binding IsDryRun}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding IsDryRun, Converter={StaticResource DryRunColorConverter}}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Close" Width="80" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
